# Task: TP-151 - Update install tutorial for current architecture

**Created:** 2026-04-07
**Size:** M

## Review Level: 0 (None)

**Assessment:** Documentation-only changes to one markdown file. No code, no config, no security surface.
**Score:** 0/8 — Blast radius: 0, Pattern novelty: 0, Security: 0, Reversibility: 0

## Canonical Task Folder

```
taskplane-tasks/TP-151-docs-install-tutorial/
├── PROMPT.md   ← This file (immutable above --- divider)
├── STATUS.md   ← Execution state (worker updates this)
├── .reviews/   ← Reviewer output (created by the orchestrator runtime)
└── .DONE       ← Created when complete
```

## Mission

Update `docs/tutorials/install.md` to reflect the current Taskplane architecture. The `/task` command no longer exists — remove all references. Runtime V2 uses direct subprocess execution (no tmux dependency) — remove tmux as a prerequisite and all tmux references. Configuration is now `taskplane-config.json` (JSON) — update config references. Write everything from the perspective of what exists today with no deprecation notices or historical context.

## Dependencies

- **None**

## Context to Read First

**Tier 2 (area context):**
- `taskplane-tasks/CONTEXT.md`

**Tier 3 (load only if needed):**
- `README.md` — ground truth for install flow, prerequisites, and commands

## Environment

- **Workspace:** `docs/tutorials/`
- **Services required:** None

## File Scope

- `docs/tutorials/install.md`

## Steps

### Step 0: Preflight

- [ ] Read `docs/tutorials/install.md` fully and catalog all stale references
- [ ] Read root `README.md` for current prerequisites and install flow

### Step 1: Update docs/tutorials/install.md

Apply these changes throughout the file:

- [ ] **Prerequisites section:** Remove tmux from the prerequisites list entirely. The required dependencies are: Node.js 22+, pi, Git. That's it. Remove the entire "Installing tmux" subsection
- [ ] **Remove all `/task` references:** Remove "Optional single-task check" section, any mentions of `/task` in verification steps, and any text that says "verifies that `/task` and `/orch` are available" (only `/orch` exists)
- [ ] **Update config references:** Change references from `.pi/task-runner.yaml` and `.pi/task-orchestrator.yaml` to `.pi/taskplane-config.json`. The YAML files may still be generated by init but JSON is the canonical config. Update the "Repo Mode" scaffolding list to reflect current init output
- [ ] **tmux detection section:** Remove the entire "tmux Detection" subsection that describes init checking for tmux and setting spawn_mode. `spawn_mode` is always `subprocess` now
- [ ] **Update "Verify Commands" section:** Remove `/task` from the verification steps. The verification should show `/orch` and `/orch-plan all` only
- [ ] **Update "Quick Smoke Test" section:** Remove the "Optional single-task mode" at the end. The smoke test should use `/orch` exclusively
- [ ] **Troubleshooting section:** Remove the "`/task` is unknown inside pi" item. Remove "tmux missing or not found" item. Keep other troubleshooting items that are still relevant
- [ ] **Remove the tip about YAML vs JSON:** The tip suggesting users "can also use a single `.pi/taskplane-config.json`" is backwards now — JSON is primary. Either remove the tip or invert it (mention YAML files are legacy fallback)
- [ ] **Update "Next Step" link** if needed

### Step 2: Documentation & Delivery

- [ ] Verify all internal doc links resolve correctly
- [ ] Discoveries logged in STATUS.md

## Documentation Requirements

**Must Update:**
- `docs/tutorials/install.md` — remove `/task`, tmux, and YAML-primary references

**Check If Affected:**
- None (other tutorials reference install.md but via simple links that won't break)

## Completion Criteria

- [ ] All steps complete
- [ ] Zero references to `/task`, `/task-status`, `/task-pause`, or `/task-resume`
- [ ] Zero references to tmux as a prerequisite or dependency
- [ ] Config references point to `taskplane-config.json` as primary
- [ ] All markdown links resolve

## Git Commit Convention

- **Step completion:** `docs(TP-151): complete Step N — description`
- **Bug fixes:** `fix(TP-151): description`
- **Hydration:** `hydrate: TP-151 expand Step N checkboxes`

## Do NOT

- Expand task scope — add tech debt to CONTEXT.md instead
- Mention `/task` in any context
- Mention tmux as a prerequisite, dependency, or install step
- Modify any files outside `docs/tutorials/install.md`
- Skip tests
- Commit without the task ID prefix in the commit message

---

## Amendments (Added During Execution)
